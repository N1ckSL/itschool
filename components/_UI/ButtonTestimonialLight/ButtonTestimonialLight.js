import { Container, Title } from "./ButtonTestimonialLight.style"


export const ButtonTestimonialLight = ({children}) => {
 return (
    <Container>
        <Title>
            {children}
        </Title>
    </Container>
 )   
}