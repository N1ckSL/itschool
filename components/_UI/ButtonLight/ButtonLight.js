import { Container, Title } from "./ButtonLight.style"


export const ButtonLight = ({children}) => {
 return (
    <Container>
        <Title>
            {children}
        </Title>
    </Container>
 )   
}